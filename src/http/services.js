import { services as auth } from '../modules/auth'
import { services as users } from '@/pages/users'
import { services as showUser } from '@/pages/user-edit'

export default {
  auth,
  users,
  showUser
}
